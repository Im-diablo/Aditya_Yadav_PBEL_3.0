# ============================================================
# AI-Powered Financial Fraud Detection System
# requirements.txt — Python 3.10+
# ============================================================

# ── Core ML ──────────────────────────────────────────────────
numpy>=2.0                  # required by shap>=0.52
pandas>=2.2.0               # compatible with numpy>=2
scikit-learn>=1.3.0
xgboost>=3.0.0              # 3.x changes leaf format; matched by shap>=0.52
shap>=0.52.0                # 0.52+ fixes XGBoost 3.x scientific-notation bug
scipy>=1.13.0               # compatible with numpy>=2
# imbalanced-learn removed — SMOTE was replaced by scale_pos_weight=577

# ── NLP ───────────────────────────────────────────────────────
transformers>=4.33.0
torch>=2.1.0
tokenizers>=0.13.3

# ── Computer Vision ───────────────────────────────────────────
torchvision>=0.16.0
opencv-python>=4.8.0.76
Pillow>=10.0.0

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.9.0           # 3.9+ compiled against numpy>=2
seaborn>=0.13.0             # 0.13+ supports numpy>=2
plotly>=5.16.1

# ── Web Server (FastAPI + Uvicorn) ────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # required for file upload (UploadFile)

# ── Utilities ─────────────────────────────────────────────────
joblib>=1.3.2
tqdm>=4.66.1
